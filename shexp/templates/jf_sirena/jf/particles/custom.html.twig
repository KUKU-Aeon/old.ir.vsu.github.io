{% extends '@nucleus/partials/particle.html.twig' %}

{% block particle %}
    <div class="platform-content">{% set html = particle.twig ? gantry.theme.compile(particle.html) : particle.html %}
    {{ (particle.filter ? gantry.platform.filter(html) : html)|html|raw }}</div>
{% endblock %}